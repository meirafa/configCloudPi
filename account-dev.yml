server:
  port: 8087
  servlet:
    context-path: /account/

spring:
  application:
    name: account
  config:
    import: optional:configserver:http://localhost:8888/
  
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://root@localhost:3306/pi_final_g2
    username: dh
    password: 123
    #    url: jdbc:mysql://host.docker.internal:3306/user
    #    username: root
    #    password: root
    #    url: jdbc:mysql://localhost:3306/pi_final_g2
    # username: user
    # password: password
    #url: jdbc:mysql://${DB_HOST_AWS}:${DB_PORT}/${DB_DATABASE}
    #username: ${DB_USER_AWS}
    #password: ${DB_PASSWORD_AWS}
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
